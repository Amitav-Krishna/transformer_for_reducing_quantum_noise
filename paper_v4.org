#+BIBLIOGRAPHY:references.bib
#+LATEX_HEADER: \usepackage{subcaption}
#+LATEX_HEADER: \usepackage[round,authoryear]{natbib}
#+LATEX_HEADER: \bibliographystyle{plainnat}
#+cite_export: natbib
#+OPTIONS: toc:nil
#+TITLE: Hierarchical Attention for Scalable Quantum State Denoising
#+AUTHOR: Amitav Krishna

* Abstract

We apply Transformer architectures to quantum density matrix denoising. A natural tokenization strategy---treating each matrix element as a token---scales quadratically with system size, becoming infeasible beyond 5-6 qubits. We introduce hierarchical patch-based tokenization that maintains a fixed token count (64) regardless of matrix dimension, enabling attention on larger quantum systems. On 5-qubit density matrices ($32\times 32$), our hierarchical Transformer achieves $2.25\times$ improvement over the noisy baseline, outperforming a parameter-matched MLP by 10% (0.375 vs 0.342 Uhlmann fidelity). Ablations with $5\times$ wider and $2\times$ deeper MLPs confirm the advantage is architectural, not capacity-based. However, scaling to 8 qubits ($256\times 256$) with $32\times 32$ patches fails: both models achieve near-baseline fidelity, revealing an information bottleneck when patches become too large. Our results establish that attention provides useful inductive bias for quantum state denoising when patch sizes preserve sufficient local structure, while identifying the limits of naive hierarchical scaling.

* Introduction

** The Scaling Problem

A natural approach to applying Transformers to density matrix denoising is element-wise tokenization---treating each matrix element as a token. However, this strategy does not scale:

| Qubits | Matrix Size | Tokens | Attention Ops |
|--------|-------------|--------|---------------|
| 5      | 32x32       | 1,024  | ~1M           |
| 6      | 64x64       | 4,096  | ~16M          |
| 8      | 256x256     | 65,536 | ~4B           |

At 8 qubits, element-wise attention is computationally infeasible.

** Our Contribution

We introduce *hierarchical patch-based tokenization* that maintains a fixed token count (64) regardless of system size:
- 5-qubit: $4\times 4$ patches on $32\times 32$ matrix $\to$ 64 tokens
- 8-qubit: $32\times 32$ patches on $256\times 256$ matrix $\to$ 64 tokens

This enables scaling to larger quantum systems while preserving the attention mechanism's ability to capture non-local correlations.

We further demonstrate through capacity and depth ablations that the Transformer's advantage is architectural, not attributable to parameter count or network depth.

** Paper Structure

- Section 2: Related work on scalable attention and quantum ML
- Section 3: Hierarchical architecture and training protocol
- Section 4: Results---baselines, ablations, and $5\to 8$ qubit scaling
- Section 5: Discussion and limitations
- Section 6: Conclusion

* Related Work

** Scalable Attention Mechanisms

TODO: Cite Linformer, Performer, BigBird, etc. for linear attention approximations.

** Hierarchical Vision Transformers

TODO: Cite ViT, Swin Transformer for patch-based tokenization in vision.

** Quantum Machine Learning for Error Correction

TODO: Brief recap of transformer-QEC and other quantum ML approaches.

* Methods

** Dataset

*** 5-Qubit Dataset

- Matrix size: $32 \times 32$ (1024 complex elements)
- 100,000 (noisy, clean) pairs
- 5 noise types $\times$ 4 noise levels = 20 cells, 5000 samples/cell
- Noise types: depolarizing, amplitude damping, phase damping, bit-flip, mixed
- Noise levels: $p \in \{0.05, 0.10, 0.15, 0.20\}$
- Precision: float64 throughout

*** 8-Qubit Dataset

- Matrix size: $256 \times 256$ (65,536 complex elements)
- 100,000 (noisy, clean) pairs
- Same noise distribution as 5-qubit
- Storage: ~50GB (streamed during training)

*** Data Split

80/10/10 train/val/test, seed=42 for reproducibility.

** Hierarchical Transformer Architecture

*** Patch Embedding

The input density matrix is divided into non-overlapping patches, each embedded via a linear projection:

- 5-qubit: $4\times 4$ patches $\to$ 64 tokens, each patch has 32 complex elements (64 real values)
- 8-qubit: $32\times 32$ patches $\to$ 64 tokens, each patch has 2048 complex elements (4096 real values)

Embedding dimension: 128 for both scales.

*** Encoder-Decoder Structure

- Encoder: 4 transformer layers, 8 attention heads, FFN dimension 512
- Decoder: 4 transformer layers, 8 attention heads, cross-attention to encoder
- Total parameters: ~1.09M (5-qubit), ~1.61M (8-qubit)

The parameter difference comes from the patch embedding/unembedding layers, which scale with patch size.

*** Patch Unembedding

Each decoder token is projected back to its corresponding patch via a linear layer, then patches are reassembled into the full matrix.

** Hierarchical MLP Architecture (MLP-Mixer Style)

For fair comparison, we use an MLP with identical patch embedding/unembedding as the Transformer, replacing only the attention mechanism:

- *Token mixing*: MLP that mixes across patches (replaces attention)
- *Channel mixing*: Per-patch MLP (same as Transformer FFN)

This isolates the effect of attention vs. fixed learned mixing weights.

Parameter counts matched to Transformer: ~1.09M (5-qubit), ~1.61M (8-qubit).

** Ablation Models

*** Wide MLP (~5.2M params)

Same architecture as matched MLP but with $4\times$ wider hidden dimensions:
- Tests whether MLP underperformance is due to insufficient capacity

*** Deep MLP (8+8 layers, ~2.15M params)

Same architecture as matched MLP but with $2\times$ depth:
- Tests whether MLP underperformance is due to insufficient depth

** Training Protocol

| Hyperparameter | Value |
|----------------|-------|
| Optimizer | AdamW |
| Learning rate | $3\times 10^{-4}$ |
| Weight decay | $10^{-5}$ |
| Batch size | 256 |
| Max epochs | 100 |
| Early stopping | patience=15 on val loss |
| Loss function | Frobenius fidelity (cosine similarity) |

** Evaluation Metric

Uhlmann fidelity: $F(\rho, \sigma) = (\mathrm{Tr}[\sqrt{\sqrt{\rho}\sigma\sqrt{\rho}}])^2$

Computed on test set after projecting outputs to valid density matrices (Hermitianize $\to$ clamp negative eigenvalues $\to$ normalize trace).

* Results

** 5-Qubit Baselines

Table [[tab:5qubit-baselines]] presents our primary comparison between hierarchical Transformer and MLP architectures on 5-qubit density matrices.

#+NAME: tab:5qubit-baselines
#+CAPTION: 5-qubit hierarchical model performance. Both models use identical patch tokenization ($4\times 4$ patches, 64 tokens) and matched parameter counts.
| Model | Params | Uhlmann Fidelity | vs Baseline |
|-------|--------|------------------|-------------|
| Baseline (noisy input) | --- | 0.167 | $1.00\times$ |
| MLP (hierarchical) | 1.09M | 0.342 | $2.04\times$ |
| Transformer (hierarchical) | 1.09M | 0.375 | $2.25\times$ |

Both architectures substantially improve over the noisy baseline, with the MLP achieving $2.04\times$ improvement and the Transformer achieving $2.25\times$ improvement. The Transformer's 10% relative advantage over the matched MLP (0.375 vs 0.342) demonstrates that self-attention captures correlations between patches that the MLP's fixed mixing weights cannot learn.

This result is notable because both models see identical input representations---the difference lies solely in how information flows between patch tokens. The MLP applies the same learned linear transformation to mix tokens regardless of input, while attention dynamically weights patch interactions based on their content.

** Capacity Ablation (5-Qubit)

A natural hypothesis for the MLP's underperformance is insufficient capacity---perhaps the fixed mixing weights simply need more parameters to approximate attention's flexibility. We test this by scaling the MLP's hidden dimensions by $4\times$, increasing parameters from 1.09M to 5.29M.

#+NAME: tab:capacity-ablation
#+CAPTION: Capacity ablation: wider MLP with $5\times$ more parameters performs worse, not better.
| Model | Params | Uhlmann Fidelity | Notes |
|-------|--------|------------------|-------|
| MLP (matched) | 1.09M | 0.342 | Baseline MLP |
| MLP (wide) | 5.29M | 0.304 | $5\times$ more params |
| Transformer | 1.09M | 0.375 | Comparison |

The wide MLP performs *worse* than the matched MLP (0.304 vs 0.342), despite having nearly $5\times$ more parameters. This counter-intuitive result suggests that the MLP architecture has reached a fundamental expressivity limit for this task---additional capacity leads to overfitting or optimization difficulties rather than improved generalization.

The Transformer achieves higher fidelity with $5\times$ fewer parameters, confirming that the advantage is architectural rather than capacity-based. Attention's ability to dynamically route information based on input content provides an inductive bias that raw parameter count cannot replicate.

** Depth Ablation (5-Qubit)

Another hypothesis is that the MLP requires more depth to learn the compositional transformations that attention computes in a single layer. We test this by doubling the number of encoder and decoder layers from 4+4 to 8+8.

#+NAME: tab:depth-ablation
#+CAPTION: Depth ablation: deeper MLP with $2\times$ layers performs worse, not better.
| Model | Layers | Params | Uhlmann Fidelity | Notes |
|-------|--------|--------|------------------|-------|
| MLP (matched) | 4+4 | 1.09M | 0.342 | Baseline MLP |
| MLP (deep) | 8+8 | 2.15M | 0.298 | $2\times$ depth |
| Transformer | 4+4 | 1.09M | 0.375 | Comparison |

The deep MLP performs *worse* than the matched MLP (0.298 vs 0.342), with the largest performance gap of any ablation. This result is particularly striking: doubling depth actively degrades performance rather than improving it.

We attribute this to optimization difficulties. While Transformers benefit from residual connections around each attention and FFN block, the MLP-Mixer architecture's token-mixing layers create longer gradient paths without the same residual structure. Deeper networks amplify this problem, leading to degraded training dynamics.

Together, the capacity and depth ablations provide strong evidence that the Transformer's advantage stems from the attention mechanism itself---neither scaling width nor depth enables the MLP to close the gap.

** 8-Qubit Scaling

The primary motivation for hierarchical tokenization is enabling attention on larger quantum systems. We test this by training both architectures on 8-qubit density matrices ($256\times 256$, 65,536 complex elements).

#+NAME: tab:8qubit-scaling
#+CAPTION: 8-qubit scaling results. Both models fail to learn meaningful denoising at this scale.
| Model | 5-qubit Fidelity | 8-qubit Fidelity | vs 8q Baseline |
|-------|------------------|------------------|----------------|
| Baseline (noisy) | 0.167 | 0.00839 | $1.00\times$ |
| MLP (hierarchical) | 0.342 | 0.00882 | $1.05\times$ |
| Transformer (hierarchical) | 0.375 | 0.00816 | $0.97\times$ |

Both architectures maintain 64 tokens at each scale: $4\times 4$ patches for 5-qubit (32 complex values per patch) and $32\times 32$ patches for 8-qubit (2048 complex values per patch).

*Unexpected result*: Neither model learns meaningful denoising on 8-qubit data. The MLP provides marginal improvement ($1.05\times$ baseline), while the Transformer performs *worse* than leaving the input unchanged ($0.97\times$ baseline). This represents a complete failure of the hierarchical approach at 8-qubit scale.

*** Analysis of 8-Qubit Failure

Training dynamics reveal that both models are essentially stuck from the first epoch:

#+NAME: tab:training-dynamics
#+CAPTION: Validation loss progression. 8-qubit models show minimal learning compared to 5-qubit.
| Model | Initial Val Loss | Final Val Loss | Improvement |
|-------|------------------|----------------|-------------|
| Transformer 5q | 0.791 | 0.459 | 41.9% |
| MLP 5q | 0.823 | 0.433 | 47.5% |
| Transformer 8q | 0.937 | 0.903 | 3.6% |
| MLP 8q | 0.938 | 0.907 | 3.3% |

The 5-qubit models reduce validation loss by 42-48% over training, while the 8-qubit models improve by only 3-4%---barely above noise. This is not an optimization failure that more epochs would resolve; the models have converged to poor local minima.

*** Root Causes

We identify three contributing factors:

*1. Information bottleneck at patch embedding.* Each $32\times 32$ patch contains 2048 complex values (4096 real numbers) that must be compressed into a 128-dimensional embedding. This 32:1 compression ratio loses critical fine-grained structure before attention even operates. For comparison, 5-qubit patches have only 32 complex values (64 real numbers)---a 2:1 ratio that preserves much more information.

*2. Insufficient model capacity per input value.* The 5-qubit model has approximately 1000 parameters per input value (1.09M params / 1024 values), while the 8-qubit model has only 25 parameters per value (1.61M params / 65,536 values). This $40\times$ reduction in relative capacity may be below the threshold needed to learn the denoising function.

*3. Fundamentally harder denoising problem.* The 8-qubit baseline fidelity (0.008) is $20\times$ lower than the 5-qubit baseline (0.167). This reflects both the exponentially larger Hilbert space and the compounding effect of per-qubit noise. The denoising task may be qualitatively different at this scale, requiring approaches beyond our current architecture.

** Training Dynamics

Figure [[fig:val-loss-curves]] visualizes the validation loss progression for all models, highlighting the stark contrast between 5-qubit and 8-qubit learning dynamics.

#+NAME: fig:val-loss-curves
#+CAPTION: Validation loss curves. 5-qubit models (solid lines) show steady improvement over 100 epochs, while 8-qubit models (dashed lines) plateau immediately with minimal learning.
[[file:figures/val_loss_curves.pdf]]

The 5-qubit curves follow the expected pattern: rapid initial improvement followed by gradual convergence, with the MLP slightly outpacing the Transformer in loss reduction (though the Transformer achieves higher Uhlmann fidelity, indicating better generalization). The 8-qubit curves are nearly flat---both models are stuck from epoch 1, confirming that the failure is architectural rather than a training duration issue.

** Timing Comparison

#+NAME: tab:timing
#+CAPTION: Training time per epoch (RTX A4000, batch size 256).
| Model | 5-qubit | 8-qubit |
|-------|---------|---------|
| MLP (matched) | 226s | 1345s |
| Transformer | 190s | 1011s |

The Transformer trains faster than the MLP at both scales despite identical token counts (64). This is because the MLP's token-mixing layers require dense matrix multiplications across all tokens, while attention benefits from optimized CUDA kernels.

** VRAM Usage

#+NAME: tab:vram
#+CAPTION: Peak GPU memory usage during training.
| Model | 5-qubit | 8-qubit |
|-------|---------|---------|
| MLP (matched) | 3.1 GB | 5.0 GB |
| Transformer | 3.1 GB | 4.9 GB |

Memory usage scales modestly from 5-qubit to 8-qubit ($\sim$60% increase) because the token count remains fixed at 64---only the patch embedding layers grow with matrix size.

* Discussion

** Why Hierarchical Tokenization Works (and When It Doesn't)

Patch-based tokenization trades spatial resolution for scalability:
- Loses fine-grained element-level attention within patches
- Preserves global attention across patches (inter-patch correlations)
- Key assumption: inter-patch correlations capture most entanglement structure

This assumption holds for 5-qubit systems with $4\times 4$ patches, where the 10% Transformer advantage over MLP confirms that attention between patches provides useful inductive bias. However, 8-qubit systems with $32\times 32$ patches violate this assumption---too much information is lost at the patch embedding stage for attention to recover meaningful structure.

** Capacity vs Architecture

The ablation results definitively demonstrate that:
1. MLP underperformance is not due to insufficient parameters (wide MLP: $5\times$ params, worse performance)
2. MLP underperformance is not due to insufficient depth (deep MLP: $2\times$ layers, worse performance)
3. The attention mechanism itself captures structure that MLPs cannot

This is the central finding for 5-qubit systems: the Transformer's advantage is architectural, not capacity-based. No amount of scaling enables the MLP to match attention's ability to dynamically weight patch interactions based on input content.

** Limitations

*** Patch Size Ceiling

Our 8-qubit results expose a fundamental limitation: when patches become too large ($32\times 32$ = 2048 complex values), the information loss at embedding overwhelms any benefit from attention. The hierarchical approach requires patches small enough to preserve local structure while remaining computationally tractable.

For intermediate scales (6-7 qubits), smaller patches with more tokens may work---e.g., $8\times 8$ patches on a $64\times 64$ matrix yields 64 tokens with only 128 values per patch. We leave this exploration to future work.

*** Noise Model Simplicity

Our synthetic noise (Markovian, independent per qubit) may not capture crosstalk and non-Markovian effects in real hardware. However, attention may be well-suited to learn such correlated noise patterns.

*** Generalization to Real Hardware

All experiments use simulated quantum states and noise. Validation on actual hardware noise, which exhibits device-specific correlations and drift, remains future work.

* Conclusion

We introduced hierarchical patch-based tokenization to enable attention-based denoising of larger quantum systems. Our experiments reveal both successes and limitations of this approach.

*5-qubit success.* On 5-qubit systems ($32\times 32$ matrices), the hierarchical Transformer achieves $2.25\times$ improvement over the noisy baseline, outperforming a parameter-matched MLP by 10% relative (0.375 vs 0.342 Uhlmann fidelity). Ablations with wider ($5\times$ params) and deeper ($2\times$ layers) MLPs confirm that this advantage is architectural: the attention mechanism captures inter-patch correlations that fixed mixing weights cannot learn.

*8-qubit failure.* Scaling to 8 qubits ($256\times 256$ matrices) with $32\times 32$ patches fails completely---both models achieve near-baseline fidelity, with the Transformer performing slightly worse than leaving inputs unchanged. Analysis reveals an information bottleneck: compressing 2048 complex values per patch into 128 dimensions loses too much structure for attention to recover.

*Implications.* Hierarchical tokenization is viable when patches remain small enough to preserve local structure ($4\times 4$ patches for 5-qubit). For larger systems, alternative strategies are needed: smaller patches with more tokens, multi-scale architectures, or fundamentally different representations that avoid the density matrix bottleneck entirely.

The central finding stands: when the representation is appropriate, attention provides inductive bias for quantum state denoising that MLPs cannot match through scale alone.

* Appendix

** Model Parameter Counts

#+NAME: tab:param-counts
#+CAPTION: Detailed parameter breakdown.
| Component | 5-qubit Transformer | 8-qubit Transformer | 5-qubit MLP | 8-qubit MLP |
|-----------|---------------------|---------------------|-------------|-------------|
| Patch embed | TBD | TBD | TBD | TBD |
| Encoder | TBD | TBD | TBD | TBD |
| Decoder | TBD | TBD | TBD | TBD |
| Patch unembed | TBD | TBD | TBD | TBD |
| Total | ~1.09M | ~1.61M | ~1.09M | ~1.61M |

** Failed Approaches

During development, we explored several approaches that failed:

- *Cholesky output parameterization*: Constraining outputs to valid density matrices via Cholesky decomposition caused models to collapse to the maximally mixed state (I/d).
- *Row-based tokenization*: Treating each matrix row as a token (32 tokens for 5-qubit) underperformed element-wise and patch-based approaches.
- *Non-residual MLP*: Removing residual connections from the MLP caused catastrophic information loss, with performance worse than baseline.

* Notes :noexport:

** Status Tracking (Updated 2026-01-01)

*** Completed Training
- [X] 5-qubit dataset generation (float64)
- [X] 8-qubit dataset generation (float64)
- [X] 5-qubit Transformer training (epoch 100)
- [X] 5-qubit MLP (matched) training (epoch 100)
- [X] 5-qubit MLP (deep) training (epoch 100)
- [X] 5-qubit MLP (wide) training (epoch 100)
- [X] 8-qubit Transformer training (epoch 100)
- [X] 8-qubit MLP training (epoch 100)

*** Completed Evals (All Done)

| Model | Uhlmann Fidelity | Status |
|-------|------------------|--------|
| 5q Baseline | 0.167 | DONE |
| 5q MLP (matched) | 0.342 | DONE |
| 5q MLP (deep) | 0.298 | DONE |
| 5q MLP (wide) | 0.304 | DONE |
| 5q Transformer | 0.375 | DONE |
| 8q Baseline | 0.00839 | DONE |
| 8q MLP | 0.00882 | DONE |
| 8q Transformer | 0.00816 | DONE |

*** Checkpoint Locations (Remote Pods)

| Model | Pod IP | Port | Checkpoint Path |
|-------|--------|------|-----------------|
| transformer_5qubit | 157.157.221.29 | 20634 | /workspace/checkpoints_16/transformer_5qubit/best.pt |
| mlp_5qubit | 157.157.221.29 | 20634 | /workspace/checkpoints_16/mlp_5qubit/best.pt |
| mlp_5qubit_deep | 157.157.221.29 | 20634 | /workspace/checkpoints_16/mlp_5qubit_deep/best.pt |
| mlp_5qubit_deep | 205.196.17.100 | 9691 | /workspace/checkpoints_16/mlp_5qubit_deep/best.pt |
| mlp_5qubit_wide | 157.157.221.29 | 20634 | /workspace/checkpoints_16/mlp_5qubit_wide/best.pt |
| mlp_5qubit_wide | 157.157.221.29 | 19931 | /workspace/checkpoints_16/mlp_5qubit_wide/best.pt |
| transformer_8qubit | 157.157.221.29 | 20657 | /workspace/checkpoints_16/transformer_8qubit/best.pt |
| transformer_8qubit | 157.157.221.29 | 19975 | /workspace/checkpoints_16/transformer_8qubit/best.pt |
| mlp_8qubit | 157.157.221.29 | 20657 | /workspace/checkpoints_16/mlp_8qubit/best.pt |
| mlp_8qubit | 157.157.221.29 | 19975 | /workspace/checkpoints_16/mlp_8qubit/best.pt |

*** Local Checkpoints (OUTDATED - from pre-reproducibility-fix runs)

All local checkpoints at train_16/checkpoints_16/ are from BEFORE the reproducibility fix
(sorted file listing + local RNG). They should NOT be used for final results.

| Model | Local Path | Status |
|-------|------------|--------|
| transformer_5qubit | train_16/checkpoints_16/transformer_5qubit/best.pt | OUTDATED |
| mlp_5qubit | train_16/checkpoints_16/mlp_5qubit/best.pt | OUTDATED |
| mlp_5qubit_deep | train_16/checkpoints_16/mlp_5qubit_deep/best.pt | OUTDATED |
| mlp_5qubit_wide | train_16/checkpoints_16/mlp_5qubit_wide/best.pt | OUTDATED |
| transformer_8qubit | train_16/checkpoints_16/transformer_8qubit/best.pt | OUTDATED |
| mlp_8qubit | train_16/checkpoints_16/mlp_8qubit/best.pt | OUTDATED |

TODO: Download fresh checkpoints from pods after eval completes.

*** TODO
- [X] Wait for 8-qubit streaming evals to complete
- [X] Fill in TBD values in tables with new results
- [ ] Download fresh checkpoints from pods
- [ ] Generate figures (val loss curves, heatmaps)
- [ ] Write prose for all sections
- [ ] Decide whether to include 8-qubit negative result or focus on 5-qubit only

** Final Results Summary (2026-01-01)

*** 5-Qubit Results (SUCCESS)

| Model | Params | Uhlmann Fidelity | vs Baseline |
|-------|--------|------------------|-------------|
| Baseline (noisy) | --- | 0.167 | 1.00× |
| MLP (deep, 8 layers) | 2.15M | 0.298 | 1.78× |
| MLP (wide, 4× hidden) | 5.29M | 0.304 | 1.82× |
| MLP (matched) | 1.09M | 0.342 | 2.04× |
| Transformer | 1.09M | 0.375 | 2.25× |

Key findings:
- Transformer beats MLP by ~10% relative (0.375 vs 0.342)
- Deep MLP performs WORSE than matched MLP despite 2× params
- Wide MLP performs WORSE than matched MLP despite 5× params
- *Confirms thesis: architecture > capacity*

*** 8-Qubit Results (FAILURE)

| Model | Uhlmann Fidelity | vs Baseline |
|-------|------------------|-------------|
| Baseline (noisy) | 0.00839 | 1.00× |
| MLP | 0.00882 | 1.05× |
| Transformer | 0.00816 | 0.97× (WORSE) |

Training loss analysis:
- Transformer val loss: 0.935 → 0.904 (only 3.3% improvement over 100 epochs)
- MLP val loss: 0.938 → 0.902 (only 3.8% improvement over 100 epochs)
- Both models essentially stuck; neither learned meaningful structure

Root cause hypotheses:
1. 32×32 patches too large (2048 complex values each)
2. Model capacity insufficient: only ~25 params/value vs ~1000 for 5-qubit
3. 8-qubit noise fundamentally harder: baseline 20× lower than 5-qubit

*** Implications for Paper

The 8-qubit failure is disappointing but scientifically interesting. Options:
1. Focus paper on 5-qubit results only (clear success story)
2. Include 8-qubit as negative result with analysis of failure modes
3. Design larger 8-qubit model (smaller patches, more params) and retrain

** Key Differences from Paper v3

1. *Hierarchical tokenization*: Patches instead of elements
2. *Larger models*: ~1.09M params vs ~120k (needed for patch embedding)
3. *8-qubit scaling*: New experiment, not in v3
4. *Capacity/depth ablations*: More systematic than v3's wide MLP experiment
5. *float64 throughout*: Numerical stability for Uhlmann fidelity

** RunPod Specs Used for Training

| Pod | GPU | vCPUs | RAM | Container Disk | Volume Storage | Purpose |
|-----|-----|-------|-----|----------------|----------------|---------|
| 5q combo | RTX A4000 | 16 | 62 GB | 5 GB | 450 GB | 5q Transformer, 5q MLP, 5q dataset gen |
| Deep MLP | RTX A4000 | 5 | 31 GB | 20 GB | 20 GB | 5q Deep MLP training |
| Wide MLP | RTX A4000 | 16 | 62 GB | 20 GB | 20 GB | 5q Wide MLP training |
| 8q dataset gen | CPU only | 32 | 256 GB | 5 GB | 300 GB | 8q dataset generation |
| 8q MLP | RTX A4000 | 16 | 62 GB | 20 GB | 300 GB | 8q MLP training |
| 8q Transformer | RTX A4000 | 16 | 62 GB | 20 GB | 300 GB | 8q Transformer training |
